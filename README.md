# Gatsby Sample Page With Blog

This is a sample Gatsby website with a Markdown blog.

## Install

Make sure that you have the Gatsby CLI program installed:

```sh
npm install -g gatsby-cli
```

Install the dependencies:

```sh
npm install
```
